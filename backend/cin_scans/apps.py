"""App config for cin_scans."""
from django.apps import AppConfig


class CinScansConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'cin_scans'
    verbose_name = 'Scans CIN'
